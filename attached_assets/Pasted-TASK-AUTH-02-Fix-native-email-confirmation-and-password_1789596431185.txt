TASK: AUTH-02 — Fix native email confirmation and password-reset routing

Read `AMODKA-PROJECT-STATUS.md` first.

Use the completed AUTH-01 audit as authoritative context.

OBJECTIVE

Correct the native email confirmation and native password recovery flows so they return to routes the existing Amodka application actually handles.

Do not redesign authentication.

EXISTING AUTH ARCHITECTURE TO PRESERVE

- Supabase Auth
- PKCE OAuth flow
- `amodka://auth/callback` for Google/Apple OAuth
- existing `lib/oauth-callback.ts`
- existing `lib/oauth-session.ts`
- existing session storage/restoration
- existing email signup/sign-in implementation
- existing password update screen:
  `app/auth/update-password.tsx`

CURRENT DEFECT

AUTH-01 established:

1. Native email signup generates a redirect equivalent to:
   `amodka://`

2. Native callback processing expects:
   `amodka://auth/callback`

3. Native password reset currently falls back to:
   `amodka://`

4. The password update route is:
   `auth/update-password`

5. Native Supabase configuration uses:
   `detectSessionInUrl: false`

REQUIRED OUTCOME

Design and implement the smallest safe routing correction so that:

A. Native email confirmation
- returns to a deterministic Amodka deep link
- is accepted by the existing native callback/session handling
- completes the Supabase session correctly
- does not weaken exact callback validation

B. Native password recovery
- returns to a deterministic Amodka deep link
- opens the existing password-update screen
- preserves the Supabase recovery session/token correctly
- does not interfere with Google or Apple OAuth callbacks

IMPORTANT

Do not reuse the OAuth callback blindly for password recovery if doing so would confuse recovery tokens with OAuth authorization codes.

Inspect the current Supabase Auth APIs and existing route/session handling before implementing.

The solution must distinguish:

- OAuth authorization callback
- email confirmation callback
- password recovery callback

Do not accept arbitrary `amodka://` URLs.

Prefer an explicit route/path that can be validated exactly.

PROTECTED AREAS

Do NOT modify:
- Google OAuth behavior
- Apple OAuth behavior
- Recommendation Engine v3.7
- Supabase RLS
- database functions
- database policies
- database grants
- RevenueCat/payments
- notifications
- Packing Concierge
- account deletion
- store metadata
- legal documents

Do not add native Apple authentication.
Do not install authentication libraries unless an existing dependency is genuinely insufficient.

VALIDATION

Add or update focused tests for:

1. valid native email-confirmation callback
2. invalid confirmation callback
3. valid native password-recovery callback
4. invalid recovery callback
5. OAuth callback still accepts only:
   `amodka://auth/callback`
6. OAuth callback behavior is unchanged
7. arbitrary `amodka://` URLs are rejected
8. expired/invalid callback parameters are rejected
9. existing OAuth cancellation behavior remains unchanged

Run only the targeted authentication tests plus TypeScript/type checking relevant to changed files.

Do NOT run a repository-wide audit.

STOP after this task.

Return:

1. Exact files changed
2. Exact routing design implemented
3. How confirmation differs from OAuth
4. How password recovery differs from OAuth
5. Tests added/changed
6. Test results
7. Typecheck result
8. Any blocker
9. Confirmation that Google/Apple OAuth code was not changed